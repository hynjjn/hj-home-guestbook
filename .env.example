# ip_hash용 salt. 아무 랜덤 문자열이면 된다
IP_HASH_SALT=change-me

# verify가 발급하는 임시 토큰 서명용. instance가 여러 개면 전부 같은 값이어야 한다
TOKEN_SECRET=change-me

# local compose용 Postgres 비밀번호
POSTGRES_PASSWORD=devpass

# 배포에서는 Neon의 pooled endpoint(-pooler가 붙은 host)를 넣는다.
# 비워 두면 local compose의 Postgres를 본다.
DATABASE_URL=
